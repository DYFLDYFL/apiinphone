---
description: 禁止把面向开发者的说明文字写入应用 UI。所有用户界面文案必须面向最终用户。
appliesTo: "**/*.tsx,**/*.ts,**/*.html,**/*.css"
---

# 禁止开发说明写入应用 UI

## 规则

**任何构建进 APK / Web 应用的用户界面中，禁止出现面向开发者的说明文字。**

这类文字的特征（出现即违规）：

- 描述"这个功能是怎么实现的"、"为什么要这样设计"（例如："分层建局，不把所有设置堆在一页。"、"新建游戏不再单独固定顺序或串并行，下面的节点可以指定多个 AI…"、"没有单独的入口节点，第一个节点绑定的 AI 就是入口组…"）
- 对开发者提要求的祈使句（例如："改这里时注意…"、"实现时…"、"点这个可以提示解锁…"）
- 解释内部机制/字段名/参数名（例如："reasoning_effort=…"、"max_tokens…"、"节点颜色来自这里…"、"流水线节点与出边（条件按列表顺序取第一条匹配）"）
- 告知"去哪创建 Key / 怎么配置"的操作教程长文（例如："创建：GitHub → Settings → Developer settings → Personal access tokens"）
- 任何"此提示面向开发/调试"性质的占位说明

## 允许的 UI 文案

- 空态提示（"暂无文件"、"文件夹为空"）
- 操作结果反馈（"已拉取 N 个文件"、"推送失败：…"）
- 需要用户决策的确认框（"切换模型会导致上下文重新读取，推荐开新对话…"）
- 必须的输入校验提示（"工作区名称不能为空"）
- 功能标签与按钮文字

## 检查时机

1. 新增 UI 文案时自检：这段话对最终用户有用吗？还是写给开发者看的？
2. 修改游戏/工作区/设置/对话界面时，顺带检查附近是否有残留开发说明
3. 收尾构建前，可全局搜索特征词：`settings-hint` 中的长说明、`（条件按`、`可多选）`、`不再`、`自动附带`、`按列表顺序` 等

违规文案直接删除或改写为面向用户的一句话。
